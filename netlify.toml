# Static Powerhouse site (index.html at repo root — no build step)
[build]
  publish = "."
  command = ""

[dev]
  framework = "#static"
  publish = "."
  port = 8742

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
