# Static Powerhouse site (index.html at repo root — no build step)
[build]
  publish = "."
  command = ""

# SPA-style fallback (single-page app; all routes serve index.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
